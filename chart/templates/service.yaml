apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-binpacking-exporter.fullname" . }}
  labels:
    {{- include "kube-binpacking-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "kube-binpacking-exporter.selectorLabels" . | nindent 4 }}
